# Decision Log — dev-helper-mcp PRD

Canonical memory and audit trail for this PRD run. Every decision, change, assumption, and
override is recorded here as the conversation unfolds.

- **Run folder:** `_bmad-output/planning-artifacts/prds/prd-dev-helper-mcp-2026-06-19/`
- **Intent:** Create
- **Facilitator working mode:** Fast path
- **Created:** 2026-06-19
- **Owner:** Dev (jhouvenaeghel@tixeo.com)

---

## Discovery decisions

| # | Decision | Rationale / source | Lands in |
|---|----------|--------------------|----------|
| D1 | **Stakes = Internal tool** (~5–8 pages, real NFRs) | User selection during stakes calibration | PRD depth |
| D2 | **Working mode = Fast path** | User selection; I draft full PRD with `[ASSUMPTION]` tags, then iterate | Workflow |
| D3 | **Orchestration scope = Manage + display only** — the MCP never spawns or kills Claude Code agents; the user launches agents manually in each worktree | User selection; biggest scope question | PRD §5 Non-Goals, §6 MVP |
| D4 | **Dashboard = Read-only live monitoring** — no actions taken from the UI | User selection | PRD §4 Feature C |
| D5 | **Tech stack = Python** (official MCP SDK + local HTTP server for dashboard) | User selection | Addendum (technical-how) |
| D6 | **Task source = Agents self-report via MCP tools** (e.g. `create_task`, `update_task_status`) | User selection | PRD §4 Feature B |
| D7 | **Worktree lifecycle = Create (+branch) + list + remove**; no merge-back assistance in v1 | User selection | PRD §4 Feature A; merge-back → Open Questions |
| D8 | **Repo scope = Single repo at a time** (multi-repo deferred) | User selection | PRD §5 Non-Goals, §6 MVP |

## Research-derived positioning decisions

| # | Decision | Rationale / source |
|---|----------|--------------------|
| R1 | Position as **MCP-native control plane** — the open niche; competitors are human-driven TUI/desktop apps | Landscape research digest (2026) |
| R2 | Explicitly position as **complement to Claude Code native `--worktree`**, not a replacement | Claude Code now ships native worktree creation; differentiation = task tracking + dashboard + agent self-service |
| R3 | MCP tool calls must be **fast/synchronous** (transport timeouts ~5 min, no native async); dashboard + persistence live in the local server process, not behind blocking MCP calls | MCP protocol constraints (SEP-1391/1686 task primitive still emerging) → NFR |
| R4 | Keep the **MCP tool surface small** (clients cap ~40 tools) | MCP client limits → NFR |
| R5 | Name (but defer) industry-gap pains so they aren't silently dropped: same-file collision pre-warning; worktree bootstrap (`.env`/deps/ports) | Research flags these as unsolved across the field → Open Questions |

## Assumptions registered (tagged `[ASSUMPTION]` in PRD, indexed in §9)

| # | Assumption | To confirm? |
|---|------------|-------------|
| A1 | Dashboard + MCP server run as one local Python process; dashboard auto-refreshes (polling/SSE) | Confirm at review |
| A2 | State persisted to a local file or small SQLite DB in a known per-project dir | Confirm at review |
| A3 | Server binds `127.0.0.1` only; no auth in v1 (single-user local machine) | Confirm at review |
| A4 | ~~Worktree naming convention derived from task/branch name~~ | **RESOLVED 2026-06-19 (D9)** |
| A5 | "Single repo at a time" = server is pointed at one project root per run | Confirm at review |

## Change history

- 2026-06-19 — Workspace bound; `.decision-log.md` + `prd.md` + `addendum.md` skeletons created.
- 2026-06-19 — Discovery completed (D1–D8); landscape research completed (R1–R5); plan approved by Dev; full PRD drafted (Fast path).
- 2026-06-19 — **D9: A4 resolved.** Worktree convention = sibling dir `<repo-parent>/<repo>.worktrees/<task>/`, branches `agent/<task>`. Chosen over Claude-native mirror and in-repo namespacing to avoid nested-worktree recursion / file-watcher confusion and ease cleanup. Threaded into PRD §4.1 FR-1, §8 Q1, §9 A4 and addendum §6.
- 2026-06-19 — **Finalize reviewer gate run.** Two parallel reviewers: PRD quality rubric (all 7 dimensions *strong*, 0 critical/high) and technical-feasibility (2 critical, 3 high, 4 medium, 2 low). Reviews at `review-rubric.md`, `review-techfeas.md`.
- 2026-06-19 — **D10: MCP transport decided = Streamable HTTP, separately-launched long-lived process** (resolves feasibility criticals C1/C2). stdio rejected (ties lifetime to one session, one subprocess per agent, port contention — incompatible with persistent shared dashboard). Cascade fixes applied from the review:
  - C1/C2/H1 → PRD Glossary (Server), UJ-1 entry state, FR-13 (long-lived/separately-launched/single-instance/prints URL), addendum §1/§2 (HTTP decision, repo binding via `--repo`, N repos = N processes/ports, single-instance lockfile, Origin validation).
  - H2 → PRD FR-4 atomic one-active-task enforcement; addendum §4 (SQLite WAL + busy_timeout + partial unique index; single-writer via the one process).
  - H3 → PRD NFR-MCP-Compatibility (protocol-synchronous ≠ event-loop-blocking; git off-loop); NFR-Performance scoped to typical repo.
  - Mediums → FR-1 slug collision policy (reject, not suffix) + pre-existing dir detection; FR-3 split `force` (dirty) vs `force_unmerged_branch` (addendum §3a); FR-12 source-of-truth `git worktree list --porcelain` + reconciliation decoupled from dashboard polling; FR-9 polling baseline / SSE vNext + ≤3s target.
  - Lows → NFR-Security Origin/DNS-rebinding guard; FR-4 done-then-reuse lifecycle clarified.
  - All accepted findings were autofixed in `prd.md`/`addendum.md`; no findings deferred or ignored.
- 2026-06-19 — **Polish pass** applied to `prd.md` and `addendum.md` (structural then prose, per `doc_standards`). IDs/tags/decisions/numbers verified preserved.
- 2026-06-19 — **FINALIZED.** `prd.md` frontmatter set `status: final`. Open Questions Q2–Q5 (merge-back, collision pre-warning, worktree bootstrap, multi-repo) deferred post-v1 with owner = Dev. No phase-blockers outstanding. Next suggested workflows: `bmad-ux`, `bmad-create-architecture`, `bmad-create-epics-and-stories`.
